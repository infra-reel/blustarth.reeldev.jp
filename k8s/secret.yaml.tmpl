# このファイルはテンプレート。実際の値を設定後に apply。
# kubectl create secret generic bluestarth-secret -n bluestarth \
#   --from-literal=jwt-secret=<STRONG_RANDOM> \
#   --from-literal=discord-client-id=<ID> \
#   --from-literal=discord-client-secret=<SECRET> \
#   --from-literal=discord-admin-ids=<UID1,UID2>
apiVersion: v1
kind: Secret
metadata:
  name: bluestarth-secret
  namespace: bluestarth
type: Opaque
stringData:
  jwt-secret: "REPLACE_ME"
  discord-client-id: "REPLACE_ME"
  discord-client-secret: "REPLACE_ME"
  discord-admin-ids: "REPLACE_ME"
